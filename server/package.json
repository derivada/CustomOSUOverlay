{
  "name": "server",
  "version": "1.0.0",
  "description": "Gets player data from the osu!API",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "derivadas",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^10.0.0",
    "node-fetch": "^2.6.1",
    "ws": "^8.0.0"
  }
}
